# cppcheck suppressions for Fincept Terminal
# Qt macros (Q_OBJECT, signals, slots, emit) are not understood by cppcheck without Qt config
unknownMacro
# System includes are not provided to cppcheck
missingIncludeSystem
# unusedFunction is noisy in a large Qt project (slots are "unused" from cppcheck's view)
unusedFunction
# MarketLab fork: inherited upstream findings baselined at the fork point
# (untouched upstream code; the runner-image cppcheck build exits 1 on them).
# Entries use cppcheck's id:file:line suppression format.
# See the Phase 2+3 finalization record in Rady70/Market_Lab.
publicAllocationError:src/auth/GoogleDesktopLogin.cpp:38
identicalConditionAfterEarlyExit:src/services/sectors/SectorResolver.cpp:231
